_ merge upstream changes from main


_ document the fix in git for windows using long file paths:
	git config --global core.longpaths true


to do:


	_ implement ODS as a separate schema
		_ grant role and role privileges to ODS objects
	_ implement synonyms to resolve to ODS objects


	create a new container for the ODS app and all backend scripts
		create a crontab for each of the backend scripts and establish a standard schedule for each instance (dev, test, prod) - use environment variables for this
		
		use environment variables to specify the server endpoints (DB host, service name, etc.)
		
		use docker secrets for each of the backend scripts
		
		BDM:
			establish a new method to upload the .zip folders with the files for the data package
			
		ODS:
			update ODS to provide access to the .zip files

		
	create a new CODE project that includes the PARR Tools container
		







_ test the different deployment/restart scenarios (tested on 12/2/25)
	_ dev
		_ new deployment
			_ apex
				_ (error message) TARGET_APEX_VERSION invalid version (regexp validation - e.g. 24.0.0)
				_ (error message) TARGET_APEX_VERSION invalid version (oracle site does not have the intaller - e.g. 24.0, 9.99)
				_ valid APEX (successful and reset admin password to ${ORACLE_PWD}
					_ login to workspace
						_ internal
						_ CAS_APX_APP
						_ PICPDM
					_ apex app can be imported to/from docker (version 23.2) and OCI (version 23.2.4 [dev] and 23.2.6 [test])
					_ CAS APEX app works
					_ PARR Tools APEX app works
					_ db
						_ PDM schema works
						_ PICDM schema works
						_ PICDM_UVM_APP schema works
						_ PICDM_ODS_APP schema works
						_ PICDM_BDM_APP schema works
						_ CAS DB schema works
						_ CAS_APX_APP DB schema works
						_ DSC DB schema works
						_ login to SYS
						_ login to SYSTEM
					_ ords
						_ configure PICDM for ORDS
						_ publish XX as an endpoint
						_ confirm records can be retrieved
		_ restart
			_ change APEX version
				_ TARGET_APEX_VERSION invalid version (regexp validation - e.g. 24.0.0)
				_ (error message) downgrade (e.g. 23.1, 22.1)
				_ (error message) TARGET_APEX_VERSION invalid version (oracle site does not have the intaller - e.g. 24.0, 9.99)
				_ APEX upgrade (successful and reset admin password to ${ORACLE_PWD}
					_ login to workspace
						_ internal
						_ CAS_APX_APP
						_ PICPDM
					_ Does not redeploy APEX workspaces/apps
						_ CAS APEX app works
						_ PARR Tools APEX app works
					_ db (does not redeploy DB) 
						_ PDM schema works
						_ PICDM schema works
						_ PICDM_UVM_APP schema works
						_ PICDM_ODS_APP schema works
						_ PICDM_BDM_APP schema works
						_ CAS DB schema works
						_ CAS_APX_APP DB schema works
						_ DSC DB schema works
						_ login to SYS
						_ login to SYSTEM
					_ ords
						_ confirm records can be retrieved
			_ leave APEX version unchanged
				_ Does not redeploy APEX workspaces/apps
					_ CAS APEX app works
					_ PARR Tools APEX app works
				_ login to workspace (password unchanged)
					_ internal
					_ CAS_APX_APP
					_ PICPDM
				_ db (does not redeploy DB) 
					_ PDM schema works
					_ PICDM schema works
					_ PICDM_UVM_APP schema works
					_ PICDM_ODS_APP schema works
					_ PICDM_BDM_APP schema works
					_ CAS DB schema works
					_ CAS_APX_APP DB schema works
					_ DSC DB schema works
					_ login to SYS
					_ login to SYSTEM
				_ ords
					_ confirm records can be retrieved
	_ test (does not save database volume)
		_ apex
			_ (error message) TARGET_APEX_VERSION invalid version (regexp validation - e.g. 24.0.0)
			_ (error message) TARGET_APEX_VERSION invalid version (oracle site does not have the intaller - e.g. 24.0, 9.99) 
			_ valid APEX (successful upgrade and reset admin password to ${ORACLE_PWD}
				_ login to workspace
					_ internal
					_ CAS_APX_APP
					_ PICPDM
				_ apex app can be imported to/from docker (version 23.2) and OCI (version 23.2.4 [dev] and 23.2.6 [test])
				_ CAS APEX app works
				_ PARR Tools APEX app works
				_ db
					_ PDM schema works
					_ PICDM schema works
					_ PICDM_UVM_APP schema works
					_ PICDM_ODS_APP schema works
					_ PICDM_BDM_APP schema works
					_ CAS DB schema works
					_ CAS_APX_APP DB schema works
					_ DSC DB schema works
					_ login to SYS
					_ login to SYSTEM
				_ ords
					_ configure PICDM for ORDS
					_ publish XX as an endpoint
					_ confirm records can be retrieved

git bash commands:

	# Dev:

		# stop and remove CODE containers and delete volumes
		cd /c/Users/jesse.abdul/Documents/Version\ Control/Git/PIFSC-PARR-Tools-Containerized-Oracle-Development-Environment
		
		docker compose -f ../docker/docker-compose-dev.yml down
		docker volume rm apex-static-vol
		docker volume rm db-vol
		
		# build and deploy the development scenario of the CODE
		bash ./deployment_scripts/build_deploy_project.sh dev
	
	# Test:

		# stop and remove CODE containers and delete volumes
		cd /c/Users/jesse.abdul/Documents/Version\ Control/Git/PIFSC-PARR-Tools-Containerized-Oracle-Development-Environment
		
		docker compose -f ../docker/docker-compose-test.yml down
		docker volume rm apex-static-vol

		# build and deploy the test scenario of the CODE
		bash ./deployment_scripts/build_deploy_project.sh test

